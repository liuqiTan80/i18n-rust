# 贡献者工具链统一：锁定 stable 通道并预装 lint/格式化组件，
# 保证本地 `cargo clippy` / `cargo fmt` 与 CI 行为一致。
#
# 注意：MSRV（1.88）由 Cargo.toml 的 rust-version 声明 + CI 的 msrv job
# 显式 `cargo +1.88` 门禁保证，不受本文件影响（+toolchain 覆盖本文件）。
[toolchain]
channel = "stable"
components = ["clippy", "rustfmt"]

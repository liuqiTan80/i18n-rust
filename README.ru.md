<div align="center">

**[中文](README.md)** · **[English](README.en.md)** · **[日本語](README.ja.md)** · **[Русский](README.ru.md)** · **[Español](README.es.md)** · **[Français](README.fr.md)** · **[Deutsch](README.de.md)** · **[한국어](README.ko.md)** · **[العربية](README.ar.md)** · **[Português](README.pt.md)** · **[हिन्दी](README.hi.md)**

</div>

> ⚠️ Этот перевод может отставать от актуальной версии. Актуальная информация — в [китайской](README.md) и [английской](README.en.md) версиях.

# rzc: Многоязычный компилятор учебного диалекта Rust

Пишите программы на Rust на родном языке — rzc автоматически переведёт их в стандартный Rust и скомпилирует. Учитесь программировать, а не английский.

```rust
// src/main.ru — русский учебный диалект Rust
функция главная() {
    пусть изменяемый число = 10;
    число = число + 1;
    печатай_строку!("Число: {}", число);
}
```

```bash
$ rzc run src/main.ru
Число: 11
```

## 📦 Установка (из исходников)

rzc не предоставляет готовых онлайн-сборок — соберите его на своей машине (1–3 минуты).

### Предварительные требования

| Инструмент | Назначение | Обязательно? |
|---|---|---|
| **Инструментарий Rust** (rustc + cargo) | Сборка самого rzc | ✅ Да |
| **git** | Получение исходников | ✅ Да (можно и ZIP) |
| **Интернет** | Загрузка зависимостей при первой сборке | ✅ Да (первый раз) |
| **Node.js 18+ и npm** | Сборка расширения VS Code | По желанию (только для IDE) |
| **rust-analyzer** | Бэкенд автодополнения/диагностики IDE | По желанию (только для IDE) |

### 1. Установка инструментария Rust

Рекомендуемый способ — [rustup](https://rustup.rs) (устанавливает rustc, cargo и rustup одной командой).

- **Linux / macOS** (в терминале):

  ```bash
  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
  ```

  Затем выполните `source "$HOME/.cargo/env"` (или переоткройте терминал).

- **Windows**: скачайте [rustup-init.exe](https://rustup.rs) и следуйте мастеру (или выполните в PowerShell `winget install --id Rustlang.Rustup`).

Проверка (после переоткрытия терминала):

```bash
rustc --version   # например: rustc 1.98.0
cargo --version
```

### 2. Получение исходников и сборка

```bash
git clone https://github.com/liuqiTan80/i18n-rust.git
cd i18n-rust
cargo build --release --workspace
./target/release/rzc --version
```

Первая сборка загружает зависимости и компилирует все компоненты (1–3 минуты). Результат:

- `target/release/rzc` — консольный инструмент
- `target/release/i18n-rust-lsp` — языковой сервер (бэкенд расширения VS Code)

### 3. (По желанию) Сделать rzc глобально доступным

```bash
cargo install --path crates/cli   # локальная сборка и установка в ~/.cargo/bin
```

### 4. (По желанию) Установить rust-analyzer для функций IDE

```bash
rzc install toolchain --ra-only --force
```

### 5. (По желанию) Сборка расширения VS Code

Требуются Node.js 18+ и npm ([nodejs.org](https://nodejs.org)):

```bash
cd tools/vscode-extension
npm ci
npm run package    # создаёт i18n-rust-<версия>.vsix
```

Установите `.vsix` через «Install from VSIX...» в VS Code; языковой сервер предоставляет `rzc install lsp`.

## 🚀 Быстрый старт

```bash
rzc init мой-проект
cd мой-проект
rzc run src/main.ru
```

`rzc init` создаёт готовый каркас проекта (`Cargo.toml` + `src/main.ru`) — можно сразу запускать.

## 🛠️ Основные команды

| Команда | Описание |
|---------|----------|
| `rzc init <имя>` | Создать новый проект |
| `rzc run <файл>` | Перевести и запустить диалектный исходник |
| `rzc check <файл>` | Проверка типов с учебной диагностикой на родном языке |
| `rzc eject <файл>` | Экспорт в стандартный код Rust |
| `rzc lang list` | Список установленных языковых пакетов |
| `rzc mapping auto <crate>` | Автоматическая генерация маппингов |

## ✨ Возможности

- **Программирование на родном языке**: полные программы на Rust с ключевыми словами вашего языка
- **Многоязычность**: 10 встроенных языковых пакетов (ru/zh/de/ja/es/fr/pt/ko/ar/hi), автоопределение по расширению файла
- **Локализованная диагностика**: `rzc check` переводит ошибки rustc на язык файла, с 💡 учебными подсказками
- **Визуализация владения**: расширение VS Code (поиск `i18n-rust`) подсвечивает перемещения и повторное использование переменных
- **Полная поддержка LSP**: автодополнение, наведение, переход к определению, поиск ссылок, переименование
- **Плавный переход**: `rzc eject` экспортирует стандартный код Rust в один шаг

## 📖 Учебник

Полный учебник на китайском для начинающих (26 глав + глоссарий + 5 приложений) — см. [tutorials/](tutorials/).

## 📄 Лицензия

[MIT](https://github.com/liuqiTan80/i18n-rust/blob/main/LICENSE)

# rzc 项目第二轮深度分析与改进建议

> 日期：2026-09-04 · 基线：v0.7.0 + 已落地的 10 个整改提交
> 本轮聚焦第一轮报告未覆盖的维度：安全细节、错误处理一致性、性能热路径、发布工程。
> 前一轮已完成项（仓库卫生、Cargo workspace 配置、CLI 集成测试、unwrap 治理、
> mapping_gen/diagnostic/response_map 拆分等）不再重复列出。

---

## 总体判断

经过第一轮整改，项目工程质量已达到成熟开源项目水准：CI 七大门禁完整、
安全基础扎实（zip-slip 防护、SHA-256 校验、密钥走环境变量）、LSP 虚拟目录
的符号链接防护甚至做到了很多商业工具都不做的程度。本轮发现的问题以
**一致性缺口**为主——好的实践已经存在，但未覆盖到所有同类场景。

---

## 一、安全性（P1，均可立即实施）

### 1.1 CLI 侧临时路径缺少符号链接防护（与 LSP 不一致）⭐ 最优先

**问题**：LSP 的 `virtual_temp_dir()` 已实现完整防护（按用户名隔离、
symlink 校验拒绝、死进程残留清理，见 `crates/lsp/src/server.rs:1260`），
但 CLI 侧 3 处仍使用可预测路径且无任何校验：

| 位置 | 路径模式 | 风险 |
|---|---|---|
| `cli/src/main.rs:1019` | `rzc-run-{pid}.exe` | 直调 rustc 的编译产物落点 |
| `cli/src/mapping_gen/doc_json.rs:20` | `rzc-mapping-{crate}-{pid}` | 创建前先 `remove_dir_all` 已知路径 |
| `cli/src/lang_manager.rs:856` | `rzc-lang-{pid}-{ts}` | 语言包 ZIP 解压目录 |

多用户机器上，本地攻击者可预创建同名符号链接：`remove_dir_all` 会跟随
链接删除任意目录，写文件会覆写任意位置。单用户教学场景风险低，但
LSP 已认可此威胁模型，CLI 属于防护缺口。

**预期效果**：三处复用 LSP 的"用户名隔离 + symlink 校验"模式，消除安全
实践不一致；`mapping_gen` 的 `remove_dir_all` 改为先 `symlink_metadata`
校验再删。
**改动量**：小（约 40 行，一个可共享的 `secure_temp_dir()` 辅助函数）。
**性质**：✅ 可立即实施。

### 1.2 CLI 非测试区 7 处 `unwrap()`（Mutex 中毒 panic）

**问题**：`crates/cli/src/main.rs` 非测试区还有 7 处 `unwrap()`，主要是
`cache.lock().unwrap()`。若后台线程持锁 panic（如转译线程意外崩溃），
主线程将因中毒锁 panic 而非优雅报错。

**预期效果**：改为 `unwrap_or_else(PoisonError::into_inner)`（缓存数据
本身不会因 panic 失效），CLI 遇到意外崩溃仍能输出母语诊断而非裸 panic。
**改动量**：小（7 行）。
**性质**：✅ 可立即实施。

---

## 二、代码质量（P1，多数可立即实施）

### 2.1 错误类型不一致：`Result<_, String>` 残留

**问题**：engine 已有结构化错误体系（`error.rs` 的 `LoadError`，注释明确
写着"取代加载层旧有的 `Result<_, String>`"），`mapping_source.rs` 已迁移，
但 `diagnostic/message.rs` 的 `load_from_file`/`load_from_string`（:31,:45）
仍返回 `Result<Self, String>`——同一 crate 内新旧两套错误风格并存。

**预期效果**：调用方（CLI/LSP 的 errors.toml 加载失败提示）可程序化区分
"文件不存在"与"TOML 解析失败"，错误信息不再靠字符串匹配。
**改动量**：小（message.rs 迁移到 `LoadError` 系 + 2 处调用点适配）。
**性质**：✅ 可立即实施。

### 2.2 `clippy` 规则未落库（本地与 CI 行为可能分叉）

**问题**：CI 以 `cargo clippy -- -D warnings` 门禁，但该规则只存在于
workflow 文件里。贡献者本地默认 clippy 不开 `-D warnings`，提交后才发现
告警；且未来想调整 lint 集合需要改 workflow 而非 Cargo.toml。

**预期效果**：根 `Cargo.toml` 增加 `[workspace.lints.clippy]`（至少
`all = { level = "warn", priority = -1 }`），三个 crate 声明
`lints.workspace = true`，本地 `cargo clippy` 与 CI 行为天然一致。
**改动量**：小（10 行配置 + 可能触发的存量告警修复）。
**性质**：✅ 可立即实施。

### 2.3 `main.rs`（2202 行）模块化拆分

**问题**：clap 子命令分发（0-900 行）与编译诊断管线耦合在单文件。
非测试区 900-1450 行是内聚的一块：`DiagContext`、`translate_cargo_diagnostics`、
`run_direct_rustc`/`check_direct_rustc`、`translate_cargo_progress`、
`extract_unresolved_crates`——即"CLI 侧诊断翻译子系统"，与
`response_map/diag_text.rs` 是姊妹职责。

**预期效果**：main.rs 降至约 1500 行（仅分发 + 转译编排），诊断管线
独立可测；与 LSP 侧结构对称，后续维护心智一致。
**改动量**：中（约 700 行移动，沿用前三个拆分验证过的模式）。
**性质**：🔧 需要较大改动（纯移动但文件大，建议单独一个 PR）。

---

## 三、架构设计（P2，需要较大改动）

### 3.1 诊断翻译的"三层兜底"应收敛为两层

**现状**：诊断消息中文化存在三条路径——
1. **engine** `diagnostic/translator.rs`：权威实现（错误码表 + 消息表 +
   类型映射），CLI 与 LSP 共用；
2. **LSP** `response_map/diag_text.rs`：叠加了 ~30 对"轻量短语替换"
   兜底表（`DIAG_PHRASE_REPLACEMENTS`，按键发布频率优化过）；
3. **CLI** `translate_cargo_progress`：cargo 进度短语（Compiling/Finished）。

前两条路径语义重叠（消息表未命中时的兜底），短语集合各自维护，
新增 UI 文案时容易漂移。

**建议**：把"轻量短语兜底"下沉到 engine（作为 `DiagnosticTranslator`
的 fallback 层），LSP 只保留 `OnceLock` 初始化；CLI 的进度短语
（非诊断）可保留独立。
**预期效果**：兜底词表单点维护；CLI 未来也能享受短语兜底；减少三处
文案漂移风险。
**改动量**：中-大（涉及 engine API 扩展 + LSP 适配 + 基线验证）。
**性质**：🔧 需要较大改动，建议与 2.3 一并规划。

### 3.2 AI 多提供商抽象（功能开发，非本轮范围）

**现状**：仅 DeepSeek（OpenAI 兼容 + `DEEPSEEK_BASE_URL` 可覆盖全部
兼容端点如本地 Ollama/vLLM）。接入 Anthropic/Gemini 需各自的 Messages
API / generateContent 实现。
**性质**：🔧 功能开发。建议在 `provider` 参数上预留枚举而非环境变量
堆叠；依赖 `mapping_gen` 已拆分出的 `ai.rs` 模块，扩展成本低。

---

## 四、性能表现（P2，先补观测再优化）

### 4.1 基准覆盖不足 LSP 每键热路径

**现状**：criterion 基线仅 4 个函数——`fullwidth_scan`、`lint_scan`、
`pipeline_cache_hit`、`pipeline_no_cache`（阈值 30% 回归门禁，机制很好）。
但 LSP 的每键热路径 `TranslationCache::update_document`（每次按键全量
转译 + 行列映射构建）与 `reverse_transpile` **没有基准覆盖**——这是用户
体感最敏感的路径。

**建议**：先补 2 个基准（update_document 小/大文档、reverse_transpile），
跑出基线数据；若数字异常再做增量优化（如指纹短路），避免盲改。
**预期效果**：LSP 输入延迟有回归门禁保护；为后续优化提供依据。
**改动量**：中（基准代码 + 基线入库）。
**性质**：🔧 需要较大改动（编写基准需构造真实文档场景）。

### 4.2 已有的性能优化值得肯定（无需改动）

逐键预取条目避免重复锁（诊断 10-50 条复用无锁纯函数）、
`user_tokens` 白名单 Arc 共享懒加载、短语表 `OnceLock` 缓存、
转译产物持久缓存与语境指纹失效——这些设计已到位，本轮未发现
明显的热路径浪费。本地 `target/` 11 GB 建议用户自行决定是否
`cargo clean`（本机已装工具链，清后可重建）。

---

## 五、可维护性（P1 一项，P2 两项）

### 5.1 engine/lsp crate 缺 `readme` 字段 ⭐ 五分钟修复

**问题**：`crates/engine/Cargo.toml` 与 `crates/lsp/Cargo.toml` 无
`readme` 字段，发布到 crates.io 后页面无 README（仅 rzc 有）。engine
作为对外发布的库 crate，crates.io 页面就是它的文档门面。

**预期效果**：crates.io 页面完整呈现；方法是在两 crate 目录放简版
README 或引用根 README（crate 打包需同步 `include` 白名单）。
**改动量**：极小。**性质**：✅ 可立即实施。

### 5.2 缺少 `rust-toolchain.toml`（有取舍）

**问题**：MSRV 声明 1.88 且 CI 有门禁，但贡献者本地无版本锁定——
用了 1.98 才有的 API 时只能等 CI 报错。
**建议**：两难取舍：pin `1.88` 会强制所有贡献者降级（edition 2024
特性不可用风险）；pin `stable` 等于没锁。**推荐折中**：不加文件，
在 `cargo build` 兜底错误信息上已有提示（现状可接受），或仅添加
`rust-toolchain.toml` 指定 `stable` + `components = ["clippy", "rustfmt"]`
保证 lint 工具链统一。
**改动量**：极小。**性质**：✅ 可立即实施（建议后者）。

### 5.3 `server.rs`（2130 行）维持不拆（结论重申）

LSP 协议主循环是单一状态机，请求分发/文档同步/翻译调度相互咬合，
拆分收益低于回归风险。除非未来要支持多后端（非 rust-analyzer），
不建议动。

---

## 六、优先级总表

| # | 改进项 | 维度 | 优先级 | 改动量 | 可否立即实施 |
|---|---|---|---|---|---|
| 1.1 | CLI 临时路径符号链接防护统一 | 安全 | 高 | 小 | ✅ 立即 |
| 2.1 | message.rs 迁移到结构化 LoadError | 代码质量 | 高 | 小 | ✅ 立即 |
| 1.2 | CLI 7 处 unwrap 中毒锁处理 | 安全/质量 | 中 | 小 | ✅ 立即 |
| 5.1 | engine/lsp 补 readme 字段 | 可维护性 | 中 | 极小 | ✅ 立即 |
| 2.2 | [workspace.lints] 落库 | 可维护性 | 中 | 小 | ✅ 立即 |
| 5.2 | rust-toolchain.toml（stable + 组件） | 可维护性 | 低 | 极小 | ✅ 立即 |
| 2.3 | main.rs 诊断管线抽模块 | 代码质量 | 中 | 中-大 | 🔧 较大改动 |
| 3.1 | 轻量短语兜底下沉 engine | 架构 | 中 | 中-大 | 🔧 较大改动 |
| 4.1 | LSP 热路径基准补齐 | 性能 | 中 | 中 | 🔧 较大改动 |
| 3.2 | AI 多提供商 | 架构/功能 | 低 | 大 | 🔧 功能开发 |
| 5.3 | server.rs 拆分 | 可维护性 | — | — | ❌ 建议不做 |

**推荐实施顺序**：1.1 → 2.1 → 1.2 → 5.1 → 2.2 → 5.2（一轮小提交即可全部
完成，合计约 100 行改动）→ 2.3 与 3.1 合并为一个专项 PR → 4.1 作为独立
PR 先补观测。

---

## 七、值得保持的亮点（本轮新增确认）

- **LSP 虚拟目录防护**：用户隔离 + PID 后缀 + symlink 校验 + 跨平台死进程
  残留清理（Windows 回退 mtime 启发式）——教科书写法
- **基准回归门禁**：criterion 基线入库 + 30% 阈值脚本对比，性能回归有护栏
- **临时产物路径防注入**：虚拟文件名只保留合法标识符字符，防特殊字符注入
- **编码健壮性**：语言包加载自动尝试 GB18030/SHIFT_JIS/BIG5（encoding_rs）
- **发布工程**：release.yml 固定 crates.io 发布顺序（engine → lsp → rzc），
  与 path 依赖转换要求一致

---

## 八、整改落地记录（2026-09-04，3 个提交）

| # | 建议 | 结果 | 提交 |
|---|---|---|---|
| 1.1 | CLI 临时路径符号链接防护 | ✅ 已实施：新增 `cli/src/temp_guard.rs`（`safe_user_segment` + `secure_temp_path`），3 处调用点统一接入并加入用户隔离段 | `1759a48` |
| 1.2 | CLI 中毒锁 unwrap | ✅ 已实施：7 处 `lock().unwrap()` → `unwrap_or_else(PoisonError::into_inner)`，CLI 非测试区 unwrap 清零 | `1759a48` |
| 2.1 | message.rs 结构化错误 | ✅ 已实施：`LoadTarget::ErrorMessages` 变体 + `ReadFailed`/`ParseFailed` 迁移，Display 复用原消息键（输出逐字不变，调用方零改动） | `9b09661` |
| 2.2 | workspace.lints 落库 | ✅ 已实施：`[workspace.lints.clippy] all = deny` + 三 crate `lints.workspace = true` | `cdec20c` |
| 5.1 | engine/lsp 补 readme | ✅ 已实施：两 crate 各新增简版 README + `readme` 字段（engine 的 `include` 白名单同步） | `cdec20c` |
| 5.2 | rust-toolchain.toml | ✅ 已实施：`stable + clippy/rustfmt`；**同步加固** MSRV job 为显式 `cargo +1.88`（+toolchain 覆盖 toolchain 文件，防止 stable 静默绕过 MSRV 门禁——这是实施中发现的关键冲突） | `cdec20c` |
| 2.3 | main.rs 拆分 | 📋 方案已定（见 2.3 节）：900-1450 行诊断管线区抽为 `cli` 内独立模块，沿用前三次拆分模式 | 待专项 PR |
| 3.1 | 轻量短语兜底下沉 | 📋 方案已定（见 3.1 节）：下沉为 `DiagnosticTranslator` 的 fallback 层，LSP 保留 OnceLock 初始化 | 待专项 PR |
| 4.1 | LSP 热路径基准 | 📋 方案已定（见 4.1 节）：补 update_document/reverse_transpile 基准入库，按数据决定优化 | 待专项 PR |
| 3.2 | AI 多提供商 | 📋 功能开发，基于 `mapping_gen/ai.rs` 预留 provider 枚举 | 待需求确认 |
| 5.3 | server.rs 拆分 | ❌ 维持不拆（协议主循环单一状态机） | — |

**验证**：388 测试全过 · clippy `-D warnings`（现经 workspace.lints 生效）零告警 · fmt 通过 · `--locked` 一致 · 端到端冒烟（init/run/check + 列映射列号精确）通过。

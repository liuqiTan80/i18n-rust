# 第十八章：智能指针

前面的章节里，数据要么放在变量里（栈上），要么用向量、字符串动态增长（堆上）。但你不知道的是——Rust 在背后帮你管理内存。

这一章要揭开谜底：数据到底存在哪里？怎么控制它存在哪里？

---

## 18.1 栈和堆——两种仓库

> **生活类比**：栈就像你**书桌上的便签纸**——拿取很快，但空间有限。堆就像**仓库**——空间很大，但存取要花时间找位置。

```
┌── 栈（便签纸）──────────┐
│  速度快，空间小          │
│  存放：整数、浮点数、布尔 │
│  大小固定的东西           │
└─────────────────────────┘

┌── 堆（仓库）──────────────┐
│  速度慢，空间大            │
│  存放：不确定大小的东西    │
│  比如：用户输入的文字      │
│  比如：不确定有多少项的列表│
└───────────────────────────┘
```

**为什么要分两种？** 因为有些东西编译时就知道大小（比如整数永远是 4 字节），放栈上最快。但有些东西编译时不知道大小（比如用户会输入多少字），只能放堆上。

---

## 18.2 盒子——把东西放到堆上

> **生活类比**：**`盒子`** 就像一张**仓库取货单**。你把东西存在仓库里，手里拿着取货单。需要用的时候，凭取货单去仓库取。

```rust
函数 主函数() {
    // 普通变量——放在栈上
    让 栈上数字 = 5;

    // 用盒子——放在堆上
    让 堆上数字 = 盒子::新建(5);

    打印行!("栈上：{}", 栈上数字);
    打印行!("堆上：{}", 堆上数字);  // 自动从仓库取出来
}
```

逐行解释：

- 第 3 行：普通变量，存在栈上——便签纸上写着 5
- 第 6 行：**`盒子::新建(5)`** 把 5 存到堆上（仓库），手里拿着取货单
- 第 9 行：直接打印堆上的值——Rust 自动帮你用取货单去仓库取

```
盒子的工作原理：

栈上（便签纸）：
  堆上数字 → 指向堆上的地址

堆上（仓库）：
  [地址xxx] = 5

打印时：堆上数字 → 找到地址 → 取出 5
```

**为什么要用盒子？** 主要场景是：
- 数据太大，不想放在栈上
- 编译时不知道数据大小
- 想让数据在多个地方共享

---

## 18.3 递归类型——盒子的重要用途

有一种东西必须用盒子——**递归类型**（自己包含自己的类型）：

> **生活类比**：想象一面镜子对着另一面镜子——无限嵌套。编译器不知道这个结构有多大。用盒子就能解决——"先留一个取货单的位置，实际内容存在仓库里"。

```rust
枚举 列表 {
    节点(整数, 盒子<列表>),
    结束,
}

函数 主函数() {
    // 创建一个列表：1 → 2 → 3 → 结束
    让 我的列表 = 列表::节点(1,
        盒子::新建(列表::节点(2,
            盒子::新建(列表::节点(3,
                盒子::新建(列表::结束)
            ))
        ))
    );
}
```

逐行解释：

- 第 1-4 行：定义"列表"——每个节点包含一个整数和指向下一个节点的盒子
- 第 2 行：**`盒子<列表>`** 意思是"盒子里装的是下一个节点"——这样编译器就知道每个节点多大了（固定大小 + 一个取货单）
- 第 7-12 行：创建 1 → 2 → 3 的链式列表

```
链式列表在内存中：

节点1 (栈上)         节点2 (堆上)         节点3 (堆上)
┌──────────┐        ┌──────────┐        ┌──────────┐
│ 值: 1    │        │ 值: 2    │        │ 值: 3    │
│ 下一个 ──┼──→     │ 下一个 ──┼──→     │ 下一个 ──┼──→ 结束
└──────────┘        └──────────┘        └──────────┘
```

---

## 18.4 引用计数——多人共享

> **生活类比**：**`引用计数`** 就像一本**公共图书**。图书馆买一本书（数据），多个人可以借来看。每有一个人借走，计数器加 1；还回来时，计数器减 1。计数器归零时，书就被销毁。

```rust
使用 标准库::引用计数::引用计数;

函数 主函数() {
    // 创建一个共享的值
    让 共享值 = 引用计数::新建("大家好".转字符串());

    打印行!("引用计数：{}", 引用计数::强引用计数(&共享值));  // 1

    // 复制引用（不是复制数据！）
    让 引用1 = 引用计数::克隆(&共享值);
    打印行!("引用计数：{}", 引用计数::强引用计数(&共享值));  // 2

    让 引用2 = 引用计数::克隆(&共享值);
    打印行!("引用计数：{}", 引用计数::强引用计数(&共享值));  // 3

    // 引用1 离开作用域，计数减 1
    // 引用2 离开作用域，计数减 1
    // 共享值 离开作用域，计数减 1 → 归零 → 数据被销毁
}
```

逐行解释：

- 第 5 行：创建一个共享值，初始引用计数为 1
- 第 9 行：`.克隆()` 不是复制数据，而是增加一个引用——计数变成 2
- 第 12 行：再克隆一次——计数变成 3
- 当计数归零时，数据自动被销毁

```
引用计数的变化：

新建 → 计数 = 1     [数据]
克隆 → 计数 = 2     [数据] ← 两个人共享
克隆 → 计数 = 3     [数据] ← 三个人共享
销毁 → 计数 = 2     [数据] 还在
销毁 → 计数 = 1     [数据] 还在
销毁 → 计数 = 0     [数据] 被清理！
```

**为什么要用引用计数？** 因为有时候多个地方需要访问同一份数据，但你不想复制它（太浪费内存）。引用计数让你安全地共享数据。

---

## 18.5 内部可变性——只读外表，可变内心

> **生活类比**：**`单元格`** 就像一个**上了锁的日记本**。从外面看，日记本是"不可变"的（别人不能随便拿）。但日记本自带一把钥匙，打开锁就能在里面修改内容。

```rust
使用 标准库::cell::单元格;

函数 主函数() {
    // 外表不可变
    让 数据 = 单元格::新建(5);

    // 但可以从内部修改！
    数据.设置(10);
    打印行!("{}", 数据.获取());  // 10
}
```

逐行解释：

- 第 5 行：`数据` 没有标记 `可变`——外表看起来不可变
- 第 8 行：`.设置(10)` 修改了内部值——但这是允许的，因为单元格内部有"锁"来保护
- 第 9 行：`.获取()` 取出当前值

**为什么要用这个？** 有时候你需要在"不可变"的地方修改数据。比如缓存——从外面看数据没变，但内部偷偷更新了缓存。

---

## 常见错误

### 忘记用盒子导致类型无限大

```rust
// ❌ 编译器不知道这个类型有多大
// 枚举 列表 {
//     节点(整数, 列表),  // 无限嵌套！
//     结束,
// }

// ✅ 用盒子限制大小
枚举 列表 {
    节点(整数, 盒子<列表>),  // 盒子大小固定
    结束,
}
```

### 引用计数循环引用

```rust
使用 标准库::引用计数::引用计数;
使用 标准库::cell::单元格;

// ❌ 如果 A 引用 B，B 又引用 A，计数永远不归零
// 内存泄漏！
// 修复：用"弱引用"打破循环
```

---

## 练习题

### 练习 1：用盒子创建数据

用 `盒子` 在堆上创建一个整数，打印它的值。

<details>
<summary>参考答案</summary>

```rust
函数 主函数() {
    让 堆上值 = 盒子::新建(42);
    打印行!("堆上的值：{}", 堆上值);  // 42
}
```
</details>

### 练习 2：引用计数

创建引用计数，克隆两次，打印每次的引用计数。

<details>
<summary>参考答案</summary>

```rust
使用 标准库::引用计数::引用计数;

函数 主函数() {
    让 a = 引用计数::新建("共享数据".转字符串());
    打印行!("计数：{}", 引用计数::强引用计数(&a));  // 1

    让 b = 引用计数::克隆(&a);
    打印行!("计数：{}", 引用计数::强引用计数(&a));  // 2

    让 c = 引用计数::克隆(&a);
    打印行!("计数：{}", 引用计数::强引用计数(&a));  // 3
}
```
</details>

---

## 本章小结

| 概念 | 一句话 |
|------|--------|
| 栈 vs 堆 | 便签纸（快但小）vs 仓库（慢但大） |
| `盒子` | 仓库取货单——把数据放在堆上 |
| `引用计数` | 公共图书——多人共享，计数归零时销毁 |
| `单元格` | 上了锁的日记本——外表不可变，内部可修改 |

**下一章**：你将学习**并发与多线程**——让程序同时做多件事。
